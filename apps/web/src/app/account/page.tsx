import { AuthStatusCard } from '../../features/auth/components/auth-status';

export default function AccountPage() {
  return (
    <main className="mx-auto max-w-4xl px-4 py-8">
      <AuthStatusCard />
    </main>
  );
}
